# Development Constitution

1. Never hardcode league rules.
2. Never expose hidden ratings by default.
3. Never bypass the Event Engine.
4. UI never owns simulation logic.
5. Every feature requires documentation.
6. Every feature requires unit tests.
7. Every feature must fit the five engine pillars.
